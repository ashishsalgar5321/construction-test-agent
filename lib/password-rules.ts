/** Password requirements shown on sign-up and reset-password flows */
export const PASSWORD_RULES = [
  'At least 8 characters long',
  'Includes at least one letter',
  'Includes at least one number',
] as const
